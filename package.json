{
  "name": "personal-investment-advisor-root",
  "version": "1.0.0",
  "description": "Root configuration to run both Personal Investment Advisor frontend and backend concurrently",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
